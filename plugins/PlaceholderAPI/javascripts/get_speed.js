var player = BukkitPlayer;
var prev_pos = player.getLocation();

function get_speed() {
    var curr_pos = player.getLocation()
    if (prev_pos == null) {
        prev_pos = 
    }
    return "Hello, world!";
}

get_speed();